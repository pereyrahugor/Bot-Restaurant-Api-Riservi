export let adapterProvider: any;
export let groupProvider: any;

export const setAdapterProvider = (provider: any) => {
    adapterProvider = provider;
};

export const setGroupProvider = (provider: any) => {
    groupProvider = provider;
};
